

#define DEF_MAX(i, j) ((i) > (j) ? (i) : (j)) // хорошо

#define MAX(i, j) ((i) > (j) ? (i) : (j)) // плохо


#ifdef
#ifdef
#define
#define

//// asdasdas
    class Ctest
    {
        public:
        int k;
    }

    int t;
        int t;
    int t;
    int t;

{ int k;
    {
        test() {}
    }
}

int p;
